rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tbi_reports/{report} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if false;
    }
  }
}
